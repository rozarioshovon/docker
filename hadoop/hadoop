#!/bin/sh

command="$@"

if [ $command = "start" ]; then
    echo "start hadoop"
    docker-compose up -d zookeeper namenode datanode

elif [ $command = "stop" ]; then
    docker-compose kill
    docker-compose rm
fi
